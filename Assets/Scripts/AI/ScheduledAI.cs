using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;
using UnityEngine.AI;
using DecayingDev;

public class ScheduledAI : MonoBehaviour
{
    public List<Day> Days;
    private NavMeshAgent nav;
    public string bitch = "Bitch";

    private void Start()
    {
      //  DecayingDev.Action action = actions[0, 0];
        //   string args[] = period.args.ToArray();
      //  List<string> args = action.args;
       // action.action.Execute(args.ToArray(), gameObject);
        
        
    }
}
